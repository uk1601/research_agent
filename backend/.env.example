# =============================================================================
# Research Paper Analyzer - Backend Configuration
# =============================================================================

# Subconscious API (Required)
# Get your API key from https://subconscious.dev/platform/api-keys
SUBCONSCIOUS_API_KEY=your-api-key-here
SUBCONSCIOUS_ENGINE=tim-large

# ArXiv Service URL (Optional)
# URL of the ArXiv microservice - must be publicly accessible
# ARXIV_SERVICE_URL=https://your-arxiv-service.com

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Origins (comma-separated for multiple)
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# Retry Configuration
MAX_RETRIES=3
RETRY_DELAY=2.0

# Timeouts (seconds)
REQUEST_TIMEOUT=120
STREAM_TIMEOUT=300
